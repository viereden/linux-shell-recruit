# Task 08 Answer

请在这里简短回答：为什么 Shell 中 `$var` 和 `"$var"` 有时会产生不同的结果？
## > 核心原因在于有无双引号（双引号功能：阻止单词拆开和通配符展开）
### `$var`:完成变量展开后，还会进行单词分割，默认以空格，制表符，换行切分为若干个参数；还会进行通配符，波浪号展开等。当变量带有含空格文件名，会被切分成多个部分。
### `"$var"`:仅执行变量内容替换不会做上述处理（单词分割和通配符展开等），变量作为单个整体进行传递，空格也会被保留。
## 除非明确需要单词切分或通配符展开，大部分情况都需要加双引号
